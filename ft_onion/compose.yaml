services:
  ft_onion:
    # profiles: ["dev"]
    # depends_on:
    #   - frontend-dev
    build:
      dockerfile: ./hidden_service/Dockerfile
    volumes:
    - ./html:/usr/share/nginx/html
    ports:
      - "4242:443"
      - "8080:80"
